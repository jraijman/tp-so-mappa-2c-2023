#include <comunicacion.h>

// informarle a la memoria que debe crear un proceso con el nombre del archivo de pseudocódigo junto con el tamaño en bytes 
send_creo_proceso(int conexion, pcb* proceso){

    //no se que seria el nombre del archivo

    

}